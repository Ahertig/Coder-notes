// Route: /api/trash

'use strict';
var router = require('express').Router();
module.exports = router;
var mongoose = require('mongoose');

// Get notes and notebooks in the trash
router.get('/')

// Get notes in the trash

// Get notebooks in the trash

// Clean the trash